# Feature Components

특정 기능에 특화된 비즈니스 로직 컴포넌트

## 폴더 구조

```
features/
├── auth/           # 인증 관련 (LoginForm, SignupForm)
├── prompt/         # 프롬프트 관련 (PromptCard, PromptDetail)
├── user/           # 유저 관련 (UserProfile, PointBadge)
└── purchase/       # 구매 관련 (PurchaseHistory)
```
